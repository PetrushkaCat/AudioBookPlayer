package cat.petrushkacat.audiobookplayer.app.ui.components

/*
@Composable
fun BooksScannerComponentUi(component: BooksScannerComponent) {
    val scanState by component.scanState.collectAsState()

    when(scanState) {
        BooksScannerComponent.ScanState.NotScanning -> {}
        BooksScannerComponent.ScanState.Scanning -> {
            LinearProgressIndicator(modifier = Modifier.fillMaxWidth())
        }
    }
}*/
